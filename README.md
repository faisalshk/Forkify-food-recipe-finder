# Forkify-food-recipe-finder
To run the project just type this command in the terminal ( npm install) to install all the required library
then in the terminal type ( npm run start ) to run the project.
